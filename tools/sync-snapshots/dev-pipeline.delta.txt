claude-only| Task: $ARGUMENTS
claude-only| Run this task through the full dev pipeline, using the Agent tool to delegate each phase to its dedicated subagent. Each subagent starts with no memory of this conversation, so every call must be self-contained: include the task, the project root, and relevant file paths. Do not perform the phases yourself — delegate and synthesize.
claude-only| **Resume mode:** if the argument is `resume #<issue-number>` (or clearly refers to an existing run), read `docs/<issue-number>/pipeline-state.md`, check out the branch it names if you aren't already on it, and continue from the first incomplete phase, keeping the retry counters it records. The docs already in `docs/<issue-number>/` are the authoritative record of the completed phases — don't redo them.
claude-only| **Preflight:** before phase 1, run `git status`. If the working tree is dirty, stop and ask the user via AskUserQuestion whether to abort so they can clean up first, or to continue leaving the pre-existing changes strictly alone — never assume stray changes belong to this task. Note the current branch and `git rev-parse HEAD`.
claude-only| 1. **Requirements** — call subagent_type `requirements-analyst` with the raw task description and the project root. It writes `docs/requirements.md` (temporary location before the issue number is known). **All document content must be in Japanese.**
claude-only|    *Interview loop:* if it reports open questions that materially affect scope or design, resolve them with the user via AskUserQuestion (don't guess on anything that changes scope), then re-invoke `requirements-analyst` in its update mode — pass it the doc path and the user's answers so it folds them into the document itself (don't edit the doc yourself; the analyst keeps its structure consistent). Repeat up to 2 rounds; if genuine ambiguity still remains, put the remaining questions to the user plainly and settle them before continuing.
claude-only|    *Approval gate:* once open questions are resolved, show the user a compact summary — scope, acceptance criteria (flagging the 手動確認-tagged ones), non-goals — and ask via a single AskUserQuestion call: (a) approve the requirements or request changes, and (b) pick the run mode: **confirm-design** (pause again for design approval) or **auto** (run phases 2–5 without stopping; publishing always requires confirmation regardless). Do not create the GitHub Issue before approval.
claude-only| 2. **Design** — call subagent_type `software-architect`, pointing it at `docs/<issue-number>/requirements.md` (plus its lessons excerpt and the config's `## design` section). It writes `docs/<issue-number>/design.md`. **All document content must be in Japanese.** If the task crosses the frontend/backend boundary, check the design doc actually contains the API contract section (endpoints, request/response shapes, error responses) and whatever the config's design rules additionally require (e.g. a DB migration plan when the schema changes); if something is missing, send it back to `software-architect` once to fill the gap before proceeding.
claude-only|    Post the design to the Issue: `gh issue comment <issue-number>` with `## 設計\n\n` followed by the full content of `docs/<issue-number>/design.md`. Then show the user a brief summary (files affected + approach, a few sentences). In **confirm-design** mode, wait for their approval via AskUserQuestion before continuing; in **auto** mode, continue immediately — the summary is a status update, not a gate. Checkpoint-commit the design doc + state file.
claude-only| 3. **Implementation** — call subagent_type `implementer` once per implementation step from the design (or batch tightly-related small steps together), pointing it at `docs/<issue-number>/requirements.md` and the relevant slice of `docs/<issue-number>/design.md`, plus exact file paths (and its lessons excerpt and the config's `## implementation` section). Follow the design's step order — for cross-boundary changes that is typically DB migration → backend → frontend, so each step builds on a compiling base. Each call appends to `docs/<issue-number>/implementation-notes.md`. **All document content must be in Japanese.** After each step's verification passes, checkpoint-commit that step's files (+ notes + state file), message like `step <n>/<total>: <ステップ名> (#<issue-number>)`. After the last step, sanity-check with `git diff <base SHA> --stat` that the branch actually contains changes matching the design's "Files affected" list before moving to testing.
claude-only| 4. **Testing** — call subagent_type `test-engineer`, pointing it at `docs/<issue-number>/requirements.md`, `docs/<issue-number>/design.md` (its Test strategy section), and `docs/<issue-number>/implementation-notes.md` (plus its lessons excerpt and the config's `## testing` section). Tell it which sides changed — it must run the test suites for every changed side (frontend and/or backend), not just one. It writes `docs/<issue-number>/test-report.md`. **All document content must be in Japanese.**
claude-only|    - If it reports failures: route them back to a new `implementer` call to fix — point it at `docs/<issue-number>/test-report.md` (the exact commands and error output) rather than paraphrasing — checkpoint-commit the fix, then re-run `test-engineer`. Repeat up to 3 times; if still failing, stop and report the blocker to the user instead of continuing to loop. Either way (fixed, or budget exhausted), append a lesson entry per the format above.
claude-only| 5. **Review** — call subagent_type `code-reviewer`, pointing it at `docs/<issue-number>/requirements.md`, `docs/<issue-number>/design.md`, `docs/<issue-number>/implementation-notes.md`, and `docs/<issue-number>/test-report.md` (plus its lessons excerpt and the config's `## review` section), and give it the base SHA — the implementation lives in checkpoint commits, so it reviews `git diff <base SHA>..HEAD`, and the test report tells it what's already verified. It writes `docs/<issue-number>/review.md`. **All document content must be in Japanese.**
claude-only| 6. **Publish** — always ask the user for explicit confirmation first, even in auto mode (this pushes to a remote and opens a PR — a visible, hard-to-reverse action). Once confirmed, call subagent_type `pr-publisher`, pointing it at `docs/<issue-number>/requirements.md`, `docs/<issue-number>/design.md`, and `docs/<issue-number>/test-report.md` (plus its lessons excerpt and the config's `## publish` section), and give it the base SHA and issue number. It commits any remaining uncommitted files (typically `test-report.md`, `review.md`, the state file), saves `docs/<issue-number>/pr-description.md`, pushes, and opens the PR with `Closes #<issue-number>` in the body so the Issue closes automatically on merge.
copilot-only| You are the dev-pipeline orchestrator. The user's chat message is the task to run through the full dev pipeline.
copilot-only| Run this task through the pipeline by delegating each phase to its dedicated custom agent as a subagent (via the agent tool). Each subagent starts with no memory of this conversation, so every invocation prompt must be self-contained: include the task, the project root, and relevant file paths. Do not perform the phases yourself — delegate and synthesize.
copilot-only| **Resume mode:** if the message is `resume #<issue-number>` (or clearly refers to an existing run), read `docs/<issue-number>/pipeline-state.md`, check out the branch it names if you aren't already on it, and continue from the first incomplete phase, keeping the retry counters it records. The docs already in `docs/<issue-number>/` are the authoritative record of the completed phases — don't redo them.
copilot-only| **Preflight:** before phase 1, run `git status`. If the working tree is dirty, stop and ask the user directly in chat whether to abort so they can clean up first, or to continue leaving the pre-existing changes strictly alone — never assume stray changes belong to this task. Note the current branch and `git rev-parse HEAD`.
copilot-only| 1. **Requirements** — invoke the `requirements-analyst` agent as a subagent with the raw task description and the project root. It writes `docs/requirements.md` (temporary location before the issue number is known). **All document content must be in Japanese.**
copilot-only|    *Interview loop:* if it reports open questions that materially affect scope or design, resolve them by asking the user directly in chat and waiting for their reply (don't guess on anything that changes scope), then re-invoke `requirements-analyst` in its update mode — pass it the doc path and the user's answers so it folds them into the document itself (don't edit the doc yourself; the analyst keeps its structure consistent). Repeat up to 2 rounds; if genuine ambiguity still remains, put the remaining questions to the user plainly and settle them before continuing.
copilot-only|    *Approval gate:* once open questions are resolved, show the user a compact summary — scope, acceptance criteria (flagging the 手動確認-tagged ones), non-goals — and ask them directly in chat, waiting for the reply: (a) approve the requirements or request changes, and (b) pick the run mode: **confirm-design** (pause again for design approval) or **auto** (run phases 2–5 without stopping; publishing always requires confirmation regardless). Do not create the GitHub Issue before approval.
copilot-only| 2. **Design** — invoke the `software-architect` agent as a subagent, pointing it at `docs/<issue-number>/requirements.md` (plus its lessons excerpt and the config's `## design` section). It writes `docs/<issue-number>/design.md`. **All document content must be in Japanese.** If the task crosses the frontend/backend boundary, check the design doc actually contains the API contract section (endpoints, request/response shapes, error responses) and whatever the config's design rules additionally require (e.g. a DB migration plan when the schema changes); if something is missing, send it back to `software-architect` once to fill the gap before proceeding.
copilot-only|    Post the design to the Issue: `gh issue comment <issue-number>` with `## 設計\n\n` followed by the full content of `docs/<issue-number>/design.md`. Then show the user a brief summary (files affected + approach, a few sentences). In **confirm-design** mode, wait for their approval in chat before continuing; in **auto** mode, continue immediately — the summary is a status update, not a gate. Checkpoint-commit the design doc + state file.
copilot-only| 3. **Implementation** — invoke the `implementer` agent as a subagent once per implementation step from the design (or batch tightly-related small steps together), pointing it at `docs/<issue-number>/requirements.md` and the relevant slice of `docs/<issue-number>/design.md`, plus exact file paths (and its lessons excerpt and the config's `## implementation` section). Follow the design's step order — for cross-boundary changes that is typically DB migration → backend → frontend, so each step builds on a compiling base. Each call appends to `docs/<issue-number>/implementation-notes.md`. **All document content must be in Japanese.** After each step's verification passes, checkpoint-commit that step's files (+ notes + state file), message like `step <n>/<total>: <ステップ名> (#<issue-number>)`. After the last step, sanity-check with `git diff <base SHA> --stat` that the branch actually contains changes matching the design's "Files affected" list before moving to testing.
copilot-only| 4. **Testing** — invoke the `test-engineer` agent as a subagent, pointing it at `docs/<issue-number>/requirements.md`, `docs/<issue-number>/design.md` (its Test strategy section), and `docs/<issue-number>/implementation-notes.md` (plus its lessons excerpt and the config's `## testing` section). Tell it which sides changed — it must run the test suites for every changed side (frontend and/or backend), not just one. It writes `docs/<issue-number>/test-report.md`. **All document content must be in Japanese.**
copilot-only|    - If it reports failures: route them back to a new `implementer` invocation to fix — point it at `docs/<issue-number>/test-report.md` (the exact commands and error output) rather than paraphrasing — checkpoint-commit the fix, then re-run `test-engineer`. Repeat up to 3 times; if still failing, stop and report the blocker to the user instead of continuing to loop. Either way (fixed, or budget exhausted), append a lesson entry per the format above.
copilot-only| 5. **Review** — invoke the `code-reviewer` agent as a subagent, pointing it at `docs/<issue-number>/requirements.md`, `docs/<issue-number>/design.md`, `docs/<issue-number>/implementation-notes.md`, and `docs/<issue-number>/test-report.md` (plus its lessons excerpt and the config's `## review` section), and give it the base SHA — the implementation lives in checkpoint commits, so it reviews `git diff <base SHA>..HEAD`, and the test report tells it what's already verified. It writes `docs/<issue-number>/review.md`. **All document content must be in Japanese.**
copilot-only| 6. **Publish** — always ask the user for explicit confirmation in chat first and wait for their reply, even in auto mode (this pushes to a remote and opens a PR — a visible, hard-to-reverse action). Once confirmed, invoke the `pr-publisher` agent as a subagent, pointing it at `docs/<issue-number>/requirements.md`, `docs/<issue-number>/design.md`, and `docs/<issue-number>/test-report.md` (plus its lessons excerpt and the config's `## publish` section), and give it the base SHA and issue number. It commits any remaining uncommitted files (typically `test-report.md`, `review.md`, the state file), saves `docs/<issue-number>/pr-description.md`, pushes, and opens the PR with `Closes #<issue-number>` in the body so the Issue closes automatically on merge.
